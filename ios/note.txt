import PerfectMySQL

let mysql = MySQL()
let connected = mysql.connect(
    host: "your_host", // 請替換為你的 MySQL 主機名
    user: "your_username", // 請替換為你的 MySQL 用戶名
    password: "your_password", // 請替換為你的 MySQL 密碼
    db: "ImageDatabase" // 請替換為你創建的資料庫名
)

guard connected else {
    print(mysql.errorMessage())
    // 處理連接失敗的情況
    return
}


let imageBlob: [UInt8] = [] // 處理影像資料轉為二進位格式，這裡只是個例子
let pose: String = "YourPose" // 替換成實際的姿勢資訊

let query = "INSERT INTO Images (image, pose) VALUES ('\(imageBlob)', '\(pose)')"
let insertSuccess = mysql.query(statement: query)

guard insertSuccess else {
    print(mysql.errorMessage())
    // 處理插入失敗的情況
    return
}

